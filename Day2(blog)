BLOG ABOUT DIFFERENCE BETWEEN DOCUMENT AND WINDOW OBJECTS

  DOCUMENT OBJECT
  1) Document object is loaded inside the window
  2) It is the object of window property
  3) All the tags elements with attributes in HTML are part of the document 
  4)It represents any HTML window or web page that is loaded in the browser
  5)the document is part of BOM(browser object model) and DOM(document object model)

WINDOW OBJECT
  1)Window objecct is the very first object that is loaded in the browser
  2)It is the object of the browser
  3)Global objects,functions and variables of javascript are memeber of window object
  4)It represents a browser window or frame that displays the content of the webpage
  5)The Window is part of BOM not DOM
